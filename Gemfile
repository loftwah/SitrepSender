source 'https://rubygems.org'

gem 'faraday'       # HTTP client for API calls
gem 'dotenv'        # Load environment variables
gem 'redcarpet'     # Convert Markdown to HTML (optional)
gem 'mime-types'